<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sky.xposed.rimet">

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:allowBackup="false"
        android:extractNativeLibs="false">

        <!-- Settings / launcher Activity — required for LSPosed to list this module -->
        <activity
            android:name=".ui.activity.MainActivity"
            android:label="@string/app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
            </intent-filter>
        </activity>

        <!-- libxposed module declarations (replaces legacy assets/xposed_init) -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />

        <meta-data
            android:name="xposeddescription"
            android:value="钉钉助手 (DingTalk Hook)" />

        <!-- Lowered to 54 so that both modern and legacy LSPosed builds recognise this module -->
        <meta-data
            android:name="xposedminversion"
            android:value="54" />

        <!-- Hook scope: only inject into DingTalk -->
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />

        <!-- Entry point for the libxposed modern API (replaces assets/xposed_init) -->
        <meta-data
            android:name="xposedmoduleentry"
            android:value="com.sky.xposed.rimet.Main" />

    </application>

</manifest>
