<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sky.xposed.rimet">

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:allowBackup="false"
        android:extractNativeLibs="false">

        <!-- Settings / launcher Activity — required for LSPosed to list this module -->
        <activity
            android:name=".ui.activity.MainActivity"
            android:label="@string/app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
            </intent-filter>
        </activity>

        <!-- libxposed module declarations (replaces legacy assets/xposed_init) -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />

        <meta-data
            android:name="xposeddescription"
            android:value="钉钉助手 (DingTalk Hook)" />

        <!-- Must be 101 to match io.github.libxposed:api:101.0.0 -->
        <meta-data
            android:name="xposedminversion"
            android:value="101" />

        <!-- Hook scope: only inject into DingTalk -->
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />

    </application>

</manifest>
